"""Packet model (placeholder).

In future development we may store detailed per‑packet metadata (packet
number, timestamps, size, etc.). For the baseline implementation the
module is currently empty but kept for structural symmetry with the
``minquic`` package.
"""
# No concrete implementation yet – reserved for future extensions.
