"""MINQUIC package placeholder.

This package will mirror the ``quic`` package and contain the MINQUIC protocol
extensions once the QUIC baseline is verified.  It is deliberately empty for
now, serving only to satisfy imports and directory structure.
"""
