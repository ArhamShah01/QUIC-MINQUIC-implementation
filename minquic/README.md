# MINQUIC Package

The **MINQUIC** implementation will be built on top of the existing QUIC baseline.
It will reuse the majority of the QUIC codebase, modifying only the components
identified in the research paper (e.g., congestion control, packet header
format, loss detection tweaks).  This README serves as a placeholder until the
MINQUIC modules are added.
